const sum = (a,b) => {
    console.log(a+b);

}

 const cube = (n) => {
    return n*n*n;
 }
const power = (x,y) => {
    return x**y;
 }

 const power2 = x => x**2;
    x = 5;

//impleseted function
const mult = (a, b) =>(
    a * b
);
//or 
const mult2 = (a, b) => a * b;//more simplified